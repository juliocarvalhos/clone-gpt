<%= form_with(model: [@conversation, @message], url: conversation_messages_path(@conversation)) do |form| %>
  <% if @message.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this M from being saved:</h2>

      <ul>
        <% @message.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div>
    <%= form.label :mensagem %>
    <%= form.text_area :text %>
  </div>

  <%= form.submit %>
<% end %>
